export default function Findings() {
  return (
    <div
      style={{ padding: 40, cursor: "pointer" }}
      onClick={() => console.log("Findings clicked")}
    >
      Findings Page
    </div>
  );
}
